/**
 * Production AdSense publisher id (must match index.html, public/ads.txt, and CI fallback).
 * Not secret — also sent to browsers with every page.
 */
export const ADSENSE_CA_PUB_FULL = "ca-pub-5368355682383000";
